{
  "name": "ghost-cli",
  "version": "0.1.0",
  "description": "Git-native encrypted environment variables - Zero-trust secret sharing",
  "type": "module",
  "main": "src/index.ts",
  "bin": {
    "ghost": "./dist/ghost"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --compile --outfile dist/ghost",
    "test": "bun test"
  },
  "keywords": [
    "secrets",
    "encryption",
    "git",
    "env",
    "security"
  ],
  "author": "Ghost Team",
  "license": "MIT",
  "dependencies": {
    "commander": "^12.0.0",
    "yaml": "^2.3.4",
    "chalk": "^5.3.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
